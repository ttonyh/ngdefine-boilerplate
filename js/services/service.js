define(['angular'], function(ng){
	
	var module = ng.module("appGlobal");

	module.factory('delayAlert', ['$timeout', '$q', function($timeout, $q){
				
		return {
			
			
			
			
			
			
		};
	}]);
	
});
